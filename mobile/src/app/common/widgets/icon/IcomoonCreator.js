import createIconSet from 'react-native-vector-icons/lib/create-icon-set'
import glyphMap from './selection.json'

const iconSet = createIconSet(glyphMap, 'Material Icons', 'MaterialIcons.ttf')

export default iconSet
